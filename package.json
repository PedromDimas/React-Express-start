{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && npm install && npm run build-client && npm start",
    "heroku-postbuild": "cd server && npm install && npm run build-client && npm start"
  },
  "dependencies": {

  }
}
